# Intel (legacy path)

**Use instead:** [Market intelligence hub](../Market_intelligence/[[README]].md) — transcripts, newsletters, daily signal briefs, and `Market_and_deal_signals.md`.

`core/paths.py` still exposes `INTEL_DIR` / `MEETING_INTEL_DIR` for older scripts; **new** code should prefer `MARKET_INTELLIGENCE_DIR` for the hub at `06-Resources/Market_intelligence/`.
