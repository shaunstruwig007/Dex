# Copy to .env — never commit real secrets.
# Build-time identifier (optional; CI sets from git SHA)
GIT_SHA=local-dev

# Next.js public vars (add as needed from Sprint 1 onward)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
